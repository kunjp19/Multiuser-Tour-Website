const { setUpSessionMiddleware, checkCustomerMiddleware, checkAdminMiddleware } = require('./middlewares');

module.exports = {
    setUpSessionMiddleware,
    checkCustomerMiddleware,
    checkAdminMiddleware
};